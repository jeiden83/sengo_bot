function run(){
    return "No hace nada, esto es para que no explote por el slash; A implementar";
}

run.description = "Link por medio del slash";

module.exports = { run, "description": run.description}