async function run(message, args){

    return `>m`;
}

run.alias = {
    "map" : {
        "args" : ""
    },
    "mapa" : {
        "args" : ""
    }
}

run.description = 
{
    'header' : `Solo has >m con el owo`,
    'body' : undefined,
    'usage' : undefined
}

module.exports = { run }