async function run(message, args){

    return `https://tsukiweb.holofield.fr/`;
}
run.description = 
{
    'header' : 'Tsukihime',
    'body' : undefined,
    'usage' : undefined
}

module.exports = { run }