async function run(message, args){

    return `No Sasu, im the biggest fan`;
}
run.description = 
{
    'header' : 'Beat this Sasu',
    'body' : undefined,
    'usage' : undefined
}

module.exports = { run }